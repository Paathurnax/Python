# cat_name = input("what is your cats' name? ")

# print(f"good morning {cat_name}!")

prefixes = "JKLMNOPQ"
suffix = "ack"

for letter in prefixes:
    if letter == 5 or letter == 7:
        print(letter + "u" + suffix)
    else:
        print(letter + suffix)
